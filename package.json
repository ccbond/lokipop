{
  "name": "@syntsugar/lokipop",
  "version": "0.1.13",
  "description": "Experience Loki on edge runtime as effortlessly as enjoying a lollipop.",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "start": "ts-node dist/index.js",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run build",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "postversion": "git push --follow-tags",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "loki",
    "edge runtime"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ccbond/lokipop"
  },
  "private": false,
  "type": "module",
  "author": "ccbond",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ccbond/lokipop/issues"
  },
  "homepage": "https://github.com/ccbond/lokipop#readme",
  "devDependencies": {
    "@babel/preset-typescript": "^7.22.5",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.5.0",
    "husky": "^8.0.3",
    "jest": "^29.6.2",
    "jest-fetch-mock": "^3.0.3",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@babel/preset-env": "^7.22.10",
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0",
    "conventional-changelog-cli": "^3.0.0"
  }
}
